import { GhostImageDirective } from './ghost-image.directive';

describe('GhostImageDirective', () => {
  it('should create an instance', () => {
    const directive = new GhostImageDirective();
    expect(directive).toBeTruthy();
  });
});
