import { CmsLinkDirective } from './cms-link.directive';

describe('CmsLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CmsLinkDirective();
    expect(directive).toBeTruthy();
  });
});
